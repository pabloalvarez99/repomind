/**
 * Tiny free-path JavaScript fixture for multi-language plumbing.
 * Not a language benchmark — one named definition a lexical index can hit.
 */

export function foo(x) {
  return x * 2;
}

export function bar() {
  return foo(21);
}
